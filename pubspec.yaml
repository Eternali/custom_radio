name: custom_radio
description: An animatable radio button that can be customized to the max.
version: 0.0.1
author: Conrad Heidebrecht
homepage: https://github.com/eternali/custom_radio

environment:
  sdk: ">=2.0.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:

  uses-material-design: true
